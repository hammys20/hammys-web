# Hammy's Trading (AWS + Amplify + Stripe MVP)

## What this is
- Next.js storefront (public inventory)
- Stripe Checkout "Buy now"
- Admin-only area behind Cognito
- Inventory + Orders via AppSync GraphQL (@model) backed by DynamoDB

## Local dev
1) Install deps
```bash
npm install

